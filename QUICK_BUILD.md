# 🚀 Швидка інструкція зі збірки .exe

> 💡 **Для детальної інструкції дивіться [BUILD_EXE.md](BUILD_EXE.md)**

## Крок 1: Встановити PyInstaller
```powershell
pip install pyinstaller
```

## Крок 2: Запустити збірку
```powershell
.\build_exe.ps1
```

Або вручну:
```powershell
pyinstaller run_app.spec --clean
```

## Крок 3: Результат
Файл буде в `dist/LocalBrain.exe`

## Крок 4: Підготувати для запуску

Створіть папку:
```
LocalBrain/
├── LocalBrain.exe
├── .env (створіть файл з AI_PROVIDER та API ключем)
└── archetypes.yaml (скопіюйте з проекту, для редагування)
```

## Крок 5: Запуск
Подвійний клік на `LocalBrain.exe`

---

## ⚠️ Важливо

1. **Файл `.env`** має бути поруч з exe файлом (AI_PROVIDER=google_ai, GOOGLE_API_KEY=ваш_ключ)
2. **Файл `archetypes.yaml`** також має бути поруч з exe (для редагування через інтерфейс)
3. **Папки `history/`, `prompts/` та `vector_db_storage/`** створяться автоматично

---

## 🐛 Якщо щось не працює

1. Перевірте наявність `.env` файлу
2. Перевірте, що порт 8000 вільний
3. Перевірте консоль на помилки (console=True в spec файлі)

---

Детальна інструкція: `BUILD_EXE.md`








