# Інструкція по оновленню EXE файлу

## ⚠️ Важливо: Збереження даних

При оновленні EXE файлу **ваші дані зберігаються автоматично**, якщо ви дотримуєтесь правил.

### Де зберігаються дані:

1. **Історія чатів**: `history/` (поруч з EXE файлом)
2. **Векторна база даних**: `vector_db_storage/` (поруч з EXE файлом)
3. **Налаштування**: `.env` (поруч з EXE файлом)
4. **Архетипи**: `archetypes.yaml` (поруч з EXE файлом)
5. **Промпти**: `prompts/` (поруч з EXE файлом)
6. **Конфігурація**: `config.yaml` (поруч з EXE файлом)
7. **Логи**: `logs/` (поруч з EXE файлом)

**Всі дані зберігаються ПОЗА EXE файлом**, тому оновлення EXE не впливає на них.

---

## 📋 Процес оновлення EXE

### Варіант 1: Безпечне оновлення (рекомендовано)

#### Крок 1: Зупинити поточний EXE
1. Закрити додаток LocalBrain (якщо він запущений)
2. Переконатися, що процес завершено (через Диспетчер задач)

#### Крок 2: Створити резервну копію (опціонально, але рекомендується)
```powershell
# Створити папку для резервної копії
New-Item -ItemType Directory -Path "backup_$(Get-Date -Format 'yyyyMMdd_HHmmss')" -Force

# Скопіювати дані
Copy-Item -Path "history" -Destination "backup_*\history" -Recurse -Force
Copy-Item -Path "vector_db_storage" -Destination "backup_*\vector_db_storage" -Recurse -Force
Copy-Item -Path ".env" -Destination "backup_*\.env" -Force
Copy-Item -Path "archetypes.yaml" -Destination "backup_*\archetypes.yaml" -Force
Copy-Item -Path "prompts" -Destination "backup_*\prompts" -Recurse -Force
Copy-Item -Path "config.yaml" -Destination "backup_*\config.yaml" -Force
```

#### Крок 3: Замінити EXE файл
1. Видалити старий `LocalBrain.exe`
2. Скопіювати новий `LocalBrain.exe` в ту саму папку
3. Переконатися, що структура папок залишилася незмінною

#### Крок 4: Запустити новий EXE
1. Запустити `LocalBrain.exe`
2. Перевірити, що історія та база даних доступні

---

### Варіант 2: Використання скрипта оновлення

Створіть файл `update_exe.ps1` в папці з EXE:

```powershell
# Скрипт для безпечного оновлення EXE
param(
    [Parameter(Mandatory=$true)]
    [string]$NewExePath
)

Write-Host "=== Оновлення LocalBrain EXE ===" -ForegroundColor Green

# Перевірка, чи існує новий EXE
if (-not (Test-Path $NewExePath)) {
    Write-Host "Помилка: Новий EXE файл не знайдено: $NewExePath" -ForegroundColor Red
    exit 1
}

# Перевірка, чи запущений старий EXE
$process = Get-Process -Name "LocalBrain" -ErrorAction SilentlyContinue
if ($process) {
    Write-Host "Зупинка поточного процесу..." -ForegroundColor Yellow
    Stop-Process -Name "LocalBrain" -Force
    Start-Sleep -Seconds 2
}

# Створення резервної копії
$backupDir = "backup_$(Get-Date -Format 'yyyyMMdd_HHmmss')"
Write-Host "Створення резервної копії в $backupDir..." -ForegroundColor Yellow
New-Item -ItemType Directory -Path $backupDir -Force | Out-Null

# Копіювання даних
$dataFolders = @("history", "vector_db_storage", "prompts", "logs")
foreach ($folder in $dataFolders) {
    if (Test-Path $folder) {
        Copy-Item -Path $folder -Destination "$backupDir\$folder" -Recurse -Force
        Write-Host "  - Скопійовано: $folder" -ForegroundColor Gray
    }
}

$dataFiles = @(".env", "archetypes.yaml", "config.yaml")
foreach ($file in $dataFiles) {
    if (Test-Path $file) {
        Copy-Item -Path $file -Destination "$backupDir\$file" -Force
        Write-Host "  - Скопійовано: $file" -ForegroundColor Gray
    }
}

# Заміна EXE
Write-Host "Заміна EXE файлу..." -ForegroundColor Yellow
if (Test-Path "LocalBrain.exe") {
    Remove-Item "LocalBrain.exe" -Force
}
Copy-Item -Path $NewExePath -Destination "LocalBrain.exe" -Force

Write-Host "`n=== Оновлення завершено! ===" -ForegroundColor Green
Write-Host "Резервна копія створена в: $backupDir" -ForegroundColor Cyan
Write-Host "Запустіть LocalBrain.exe для перевірки" -ForegroundColor Cyan
```

**Використання:**
```powershell
.\update_exe.ps1 -NewExePath "C:\path\to\new\LocalBrain.exe"
```

---

## ✅ Перевірка після оновлення

### 1. Перевірка історії
1. Запустити LocalBrain.exe
2. Відкрити "Історія"
3. Переконатися, що всі чати на місці

### 2. Перевірка векторної бази
1. Відкрити "Векторна БД"
2. Перевірити кількість записів
3. Перевірити пошук

### 3. Перевірка налаштувань
1. Перевірити, що API ключі працюють
2. Перевірити налаштування архетипів
3. Перевірити мову інтерфейсу

---

## 🔄 Автоматичне оновлення (майбутня можливість)

В майбутньому можна додати функцію автоматичного оновлення:
- Перевірка нової версії
- Завантаження нового EXE
- Автоматичне оновлення зі збереженням даних

---

## 📁 Структура папки з EXE

```
LocalBrain/
├── LocalBrain.exe          # EXE файл (оновлюється)
├── .env                    # Налаштування (зберігається)
├── archetypes.yaml         # Архетипи (зберігається)
├── config.yaml             # Конфігурація (зберігається)
├── history/                # Історія чатів (зберігається)
│   ├── 20250109_120000_sofiya.json
│   └── ...
├── vector_db_storage/      # Векторна база (зберігається)
│   └── ...
├── prompts/                # Промпти (зберігається)
│   ├── sofiya_base.txt
│   └── ...
├── logs/                   # Логи (зберігається)
│   └── local_brain.log
└── backup_YYYYMMDD_HHMMSS/ # Резервні копії (створюються вручну)
    ├── history/
    ├── vector_db_storage/
    └── ...
```

---

## ⚠️ Важливі зауваження

### ✅ Безпечно:
- Заміна `LocalBrain.exe` на новий
- Видалення старих логів
- Оновлення промптів

### ❌ Небезпечно:
- Видалення папки `history/`
- Видалення папки `vector_db_storage/`
- Видалення файлу `.env`
- Видалення файлу `archetypes.yaml`

### 💡 Рекомендації:
1. **Завжди робіть резервну копію** перед оновленням
2. **Не видаляйте папки з даними** при оновленні
3. **Зберігайте структуру папок** незмінною
4. **Перевіряйте дані** після оновлення

---

## 🔧 Вирішення проблем

### Проблема: Історія не відображається
**Рішення:**
1. Перевірити, чи існує папка `history/`
2. Перевірити права доступу до папки
3. Перевірити формат файлів (мають бути `.json`)

### Проблема: Векторна база порожня
**Рішення:**
1. Перевірити, чи існує папка `vector_db_storage/`
2. Перевірити права доступу
3. Якщо база порожня, вона заповниться при нових чатах

### Проблема: Налаштування не зберігаються
**Рішення:**
1. Перевірити файл `.env`
2. Перевірити файл `archetypes.yaml`
3. Перевірити права доступу до файлів

### Проблема: EXE не запускається
**Рішення:**
1. Перевірити, чи немає запущених процесів
2. Перевірити антивірус (може блокувати)
3. Спробувати запустити від імені адміністратора

---

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте логи в папці `logs/`
2. Перевірте резервну копію
3. Відновіть дані з резервної копії

---

## 📝 Чеклист оновлення

- [ ] Зупинено поточний EXE
- [ ] Створено резервну копію
- [ ] Замінено EXE файл
- [ ] Запущено новий EXE
- [ ] Перевірено історію
- [ ] Перевірено векторну базу
- [ ] Перевірено налаштування
- [ ] Все працює коректно

---

**Примітка:** Ця інструкція забезпечує безпечне оновлення EXE файлу зі збереженням усіх даних користувача.







