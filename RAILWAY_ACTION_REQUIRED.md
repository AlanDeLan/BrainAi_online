# ⚠️ ОБОВ'ЯЗКОВІ ДІЇ В RAILWAY DASHBOARD

## 🚨 Проблема зараз
- ❌ Історія чатів обнуляється після деплою
- ❌ Векторна база даних не зберігається
- ❌ Дублювання записів у Vector DB UI

## ✅ Рішення

### Крок 1: Створити Volume для History (НОВИЙ!)

1. Відкрити https://railway.app
2. Обрати проект **BrainAi Online**
3. Натиснути на сервіс → **Settings**
4. Прокрутити до секції **"Volumes"**
5. Натиснути **"New Volume"**
6. Заповнити:
   - **Mount Path**: `/app/history`
   - **Name**: `chat_history`
7. Натиснути **"Add"**

### Крок 2: Перевірити Volume для ChromaDB

**Якщо вже створений** - пропустити цей крок.

**Якщо НЕ створений**:
1. Натиснути **"New Volume"** ще раз
2. Заповнити:
   - **Mount Path**: `/app/vector_db_storage`
   - **Name**: `chromadb_storage`
3. Натиснути **"Add"**

### Крок 3: Дочекатися Redeploy

Railway автоматично перезапустить деплоймент. Зачекайте 2-3 хвилини.

### Крок 4: Перевірити в логах

Відкрити **Deploy Logs** та знайти:
```
✅ Mounting volume chromadb_storage to /app/vector_db_storage
✅ Mounting volume chat_history to /app/history
```

## 📊 Після налаштування

### Що буде працювати:
- ✅ **Історія чатів зберігається** між деплоями
- ✅ **Векторна база зберігається** між деплоями
- ✅ **Записи не дублюються** (виправлено в коді)
- ✅ **Можна переглядати** чати у Vector DB UI

### Тестування:
1. Надішліть повідомлення в чат
2. Перевірте Vector DB - має показати чат
3. Зробіть будь-який git push (щоб спровокувати деплой)
4. Після деплою перевірте знову - **дані мають зберегтися!**

## 🔍 Діагностика

### Якщо історія все одно втрачається:

**Перевірте в Railway Dashboard → Settings → Volumes:**
- [ ] Volume `chat_history` створений
- [ ] Mount path точно `/app/history` (без пробілів!)
- [ ] Volume `chromadb_storage` створений  
- [ ] Mount path точно `/app/vector_db_storage`

### Якщо записи дублюються:

**Перевірте версію коду:**
```bash
curl https://brainaionline-production.up.railway.app/health
```
Має бути останній деплой з commit `e0f8171`

### Перевірити Vector DB:
```bash
curl https://brainaionline-production.up.railway.app/api/vector-db \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

Має повертати **один запис на чат** (не два).

## 📝 Що було виправлено в коді

1. ✅ Додано `get_all_chats_grouped()` - групує повідомлення за chat_id
2. ✅ Виправлено `/api/vector-db/{chat_id}` - повертає всі повідомлення чату
3. ✅ Виправлено delete - видаляє всі повідомлення чату
4. ✅ Виправлено edit - оновлює відповідь асистента
5. ✅ Додано volume для `/app/history` в railway.toml
6. ✅ Додано права доступу `chmod 777 history` в Dockerfile

## 🎯 Результат

Після виконання всіх кроків:
- Історія чатів **НЕ ОБНУЛЯЄТЬСЯ** при деплої
- Vector DB **ЗБЕРІГАЄ ВСІ ДАНІ** між деплоями
- UI показує **ПО ОДНОМУ ЗАПИСУ** на чат
- Можна **ПЕРЕГЛЯДАТИ ПОВНУ РОЗМОВУ** при кліку на чат
