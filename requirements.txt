# ================================
# Production Dependencies
# ================================

# === Web Framework ===
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
gunicorn>=21.2.0
python-multipart>=0.0.6

# === Data Validation ===
pydantic>=2.0.0
pydantic-settings>=2.1.0  # For production settings management
email-validator>=2.1.0

# === Configuration ===
python-dotenv>=1.0.0
PyYAML>=6.0.0

# === Templates & Static Files ===
jinja2>=3.1.0
aiofiles>=23.2.1

# === AI Providers ===
google-generativeai>=0.3.0
openai>=1.0.0

# === Vector Database ===
# (Legacy) chroma/faiss kept for reference; pgvector is primary now
pgvector>=0.2.5

# === Document Processing ===
python-docx>=1.0.0
PyPDF2>=3.0.0

# === Database (Production) ===
psycopg2-binary>=2.9.9  # PostgreSQL adapter for Python
sqlalchemy>=2.0.0  # ORM for database operations
alembic>=1.13.0  # Database migrations

# === Security & Authentication ===
passlib[bcrypt]>=1.7.4  # Password hashing
bcrypt>=4.0.0  # Explicit bcrypt version for Railway compatibility
python-jose[cryptography]>=3.3.0  # JWT tokens
python-multipart>=0.0.6  # For form data
cryptography>=41.0.0  # Encryption

# === Testing ===
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0
pytest-cov>=4.1.0

# === Logging & Monitoring ===
python-json-logger>=2.0.7  # Structured logging

# === Production Server ===
python-multipart>=0.0.6  # Required by FastAPI
