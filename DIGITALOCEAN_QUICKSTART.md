# 🚀 Швидкий старт на DigitalOcean App Platform

## Покрокова інструкція для розгортання BrainAi Online

---

## 📝 Передумови

- ✅ Акаунт на [DigitalOcean](https://cloud.digitalocean.com/)
- ✅ GitHub репозиторій `AlanDeLan/BrainAi_online` готовий
- ✅ API ключі Google AI (та/або OpenAI)
- ✅ Кредитна картка для оплати (~$12/міс)

---

## Крок 1: Генерація секретних ключів

Перед створенням застосунку згенеруйте ключі локально:

```powershell
# PowerShell - генерація SECRET_KEY
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | ForEach-Object {[char]$_})

# PowerShell - генерація SESSION_SECRET
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | ForEach-Object {[char]$_})
```

**Збережіть ці ключі** - вони знадобляться у Кроці 4.

---

## Крок 2: Створення застосунку на DigitalOcean

### 2.1 Відкрийте App Platform

1. Перейдіть на https://cloud.digitalocean.com/apps
2. Натисніть кнопку **Create App** (зелена кнопка у правому верхньому куті)

### 2.2 Підключіть GitHub

1. Виберіть **GitHub** як джерело
2. Натисніть **Manage Access** (якщо DigitalOcean ще не має доступу)
3. У вікні GitHub авторизації:
   - Виберіть **Only select repositories**
   - Оберіть `AlanDeLan/BrainAi_online`
   - Натисніть **Install & Authorize**

### 2.3 Налаштуйте репозиторій

1. **Repository**: `AlanDeLan/BrainAi_online`
2. **Branch**: `main`
3. **Source Directory**: `/` (корінь репозиторію)
4. ✅ Увімкніть **Autodeploy** (автоматичний деплой при git push)
5. Натисніть **Next**

### 2.4 Конфігурація ресурсів

DigitalOcean автоматично виявить `Dockerfile`. Перевірте:

- **Resource Type**: Web Service
- **Name**: `web`
- **Dockerfile Path**: `Dockerfile`
- **HTTP Port**: `8000`

Натисніть **Next**

### 2.5 Виберіть план

**Рекомендовано для старту:**
- **Plan**: Basic
- **Size**: **Basic XXS** ($5/міс) - 512MB RAM, 1 vCPU
  - Або **Basic XS** ($12/міс) - 1GB RAM (краще для стабільності)

Натисніть **Next**

---

## Крок 3: Додавання PostgreSQL бази даних

### 3.1 Створіть базу даних

1. На екрані налаштування застосунку прокрутіть до **Add Resources**
2. Натисніть **Add Resource** → **Database**
3. Виберіть **Create and attach new database**

### 3.2 Налаштуйте PostgreSQL

- **Database Engine**: PostgreSQL
- **Version**: `15`
- **Cluster Name**: `brainai-cluster`
- **Database Name**: `brainai`
- **Plan**: 
  - **Dev Database** ($7/міс) - для тестування ✅
  - Production ($15/міс) - для продакшену
- **Region**: **Frankfurt** (FRA1)

Натисніть **Create and Attach**

DigitalOcean автоматично створить змінну `DATABASE_URL`.

---

## Крок 4: Налаштування змінних середовища

### 4.1 Перейдіть до налаштувань

На екрані конфігурації застосунку знайдіть розділ **Environment Variables** (або Settings → App-Level Environment Variables після створення).

### 4.2 Додайте обов'язкові змінні

Натискайте **Edit** → **Add Variable** для кожної:

| Ключ | Значення | Тип | Encrypt? |
|------|----------|-----|----------|
| `GOOGLE_API_KEY` | Ваш ключ з [Google AI Studio](https://aistudio.google.com/app/apikey) | `SECRET` | ✅ Yes |
| `SECRET_KEY` | Згенерований у Кроці 1 | `SECRET` | ✅ Yes |
| `SESSION_SECRET` | Згенерований у Кроці 1 | `SECRET` | ✅ Yes |
| `AI_PROVIDER` | `google_ai` | `GENERAL` | ❌ No |
| `ENVIRONMENT` | `production` | `GENERAL` | ❌ No |
| `DEBUG` | `false` | `GENERAL` | ❌ No |
| `CORS_ORIGINS` | Залиште пустим (оновимо після деплою) | `GENERAL` | ❌ No |

### 4.3 Опціональні змінні (за потреби)

| Ключ | Значення | Опис |
|------|----------|------|
| `OPENAI_API_KEY` | Ваш ключ OpenAI | Якщо плануєте використовувати OpenAI |
| `RATE_LIMIT_PER_MINUTE` | `60` | Ліміт запитів |
| `RATE_LIMIT_PER_HOUR` | `1000` | Ліміт запитів |
| `LOG_LEVEL` | `INFO` | Рівень логування |

Натисніть **Save**

---

## Крок 5: Додавання Persistent Storage

### 5.1 Створіть Volume

1. У налаштуваннях застосунку знайдіть **Resources**
2. Прокрутіть до **Add Resource** → **Volume**
3. Заповніть:
   - **Name**: `vector-db-storage`
   - **Mount Path**: `/app/vector_db_storage`
   - **Size**: `1 GB` (можна збільшити пізніше)
4. Натисніть **Create and Attach**

Цей volume буде зберігати вашу векторну базу даних (ChromaDB/FAISS).

---

## Крок 6: Налаштування Health Check

1. У розділі **Settings** → **Health Checks**
2. Переконайтесь, що налаштовано:
   - **HTTP Path**: `/health`
   - **Initial Delay**: `10` seconds
   - **Period**: `30` seconds
   - **Timeout**: `5` seconds
   - **Success Threshold**: `1`
   - **Failure Threshold**: `3`

(Зазвичай DigitalOcean налаштовує це автоматично з `app.yaml`)

---

## Крок 7: Запуск деплою

### 7.1 Створіть застосунок

1. Перевірте всі налаштування
2. Натисніть **Create Resources** (внизу сторінки)

DigitalOcean почне:
- ✅ Створювати PostgreSQL кластер (~5 хвилин)
- ✅ Білдити Docker image (~3-5 хвилин)
- ✅ Деплоїти застосунок (~2 хвилини)

### 7.2 Моніторинг деплою

1. Ви автоматично потрапите на сторінку застосунку
2. Перегляньте логи: **Runtime Logs** (лівий сайдбар)
3. Слідкуйте за процесом:
   ```
   Building...
   Pushing image...
   Deploying...
   Health check passed ✓
   ```

### 7.3 Отримайте URL застосунку

Після успішного деплою:
1. Знайдіть URL у верхній частині сторінки:
   ```
   https://brainai-online-xxxxx.ondigitalocean.app
   ```
2. **Скопіюйте цей URL** - він знадобиться для CORS

---

## Крок 8: Оновлення CORS_ORIGINS

### 8.1 Додайте URL до CORS

1. Перейдіть **Settings** → **App-Level Environment Variables**
2. Знайдіть `CORS_ORIGINS` (або створіть нову)
3. Встановіть значення:
   ```json
   ["https://brainai-online-xxxxx.ondigitalocean.app"]
   ```
   (Замініть `xxxxx` на ваш реальний URL)
4. Натисніть **Save**

Застосунок автоматично перезапуститься (~1 хвилина).

---

## Крок 9: Перевірка роботи

### 9.1 Тест Health Endpoint

Відкрийте у браузері:
```
https://brainai-online-xxxxx.ondigitalocean.app/health
```

Очікувана відповідь:
```json
{
  "status": "healthy",
  "timestamp": "2025-11-14T12:00:00Z",
  "version": "1.0.0",
  "database": "connected"
}
```

### 9.2 Тест основного застосунку

1. Відкрийте головну сторінку:
   ```
   https://brainai-online-xxxxx.ondigitalocean.app
   ```

2. Перевірте функціонал:
   - ✅ Чат завантажується
   - ✅ Можна надіслати повідомлення
   - ✅ AI відповідає
   - ✅ Історія зберігається

### 9.3 Перегляд логів

У Dashboard → **Runtime Logs**:
- Переконайтесь, що немає критичних помилок
- Перевірте успішне підключення до PostgreSQL
- Перевірте ініціалізацію векторної БД

---

## Крок 10: Налаштування власного домену (опціонально)

### 10.1 Додайте домен

1. **Settings** → **Domains**
2. Натисніть **Add Domain**
3. Введіть: `brainai.yourdomain.com`

### 10.2 Налаштуйте DNS

DigitalOcean покаже CNAME запис:
```
CNAME: brainai → brainai-online-xxxxx.ondigitalocean.app
```

У вашому DNS провайдері (Cloudflare, Namecheap, etc.):
1. Створіть новий CNAME запис
2. **Name**: `brainai`
3. **Target**: `brainai-online-xxxxx.ondigitalocean.app`
4. **TTL**: Auto або 300

### 10.3 Дочекайтесь SSL

- DNS поширення: до 48 годин (зазвичай 5-30 хвилин)
- SSL сертифікат (Let's Encrypt): автоматично після DNS

### 10.4 Оновіть CORS

Додайте новий домен у `CORS_ORIGINS`:
```json
["https://brainai.yourdomain.com"]
```

---

## ✅ Чеклист успішного деплою

- [ ] Застосунок створено на DigitalOcean
- [ ] PostgreSQL база даних підключена
- [ ] Всі змінні середовища додано
- [ ] Persistent volume створено
- [ ] Health endpoint повертає `{"status": "healthy"}`
- [ ] Основний застосунок відкривається
- [ ] AI відповідає на запити
- [ ] CORS_ORIGINS оновлено з реальним URL
- [ ] Логи не показують критичних помилок
- [ ] Власний домен налаштовано (якщо потрібно)

---

## 🔧 Troubleshooting

### Проблема: Health check fails

**Рішення:**
```powershell
# Перевірте логи
doctl apps logs <APP_ID> --type run --follow

# Типові причини:
# - Порт 8000 не слухається
# - DATABASE_URL неправильна
# - Відсутні змінні середовища
```

### Проблема: Database connection error

**Рішення:**
1. Перевірте, що PostgreSQL статус = **Available**
2. Перевірте змінну `DATABASE_URL` (автоматична)
3. Перезапустіть застосунок: Actions → **Force Redeploy**

### Проблема: 502 Bad Gateway

**Рішення:**
- Збільшіть instance size до Basic XS (1GB RAM)
- Перевірте, що застосунок слухає на `0.0.0.0:8000`
- Перегляньте Runtime Logs для деталей

### Проблема: Out of memory

**Рішення:**
```yaml
# Upgrade план у Dashboard
Settings → Resources → Edit Plan → Basic XS ($12/міс)
```

---

## 💰 Підсумок вартості

**Мінімальна конфігурація:**
- App Platform Basic XXS: **$5/міс**
- PostgreSQL Dev Database: **$7/міс**
- Volume 1GB: **$0.10/міс**
- **Всього: ~$12.10/міс**

**Рекомендована конфігурація:**
- App Platform Basic XS: **$12/міс** (1GB RAM)
- PostgreSQL Dev Database: **$7/міс**
- Volume 5GB: **$0.50/міс**
- **Всього: ~$19.50/міс**

---

## 🎯 Наступні кроки

1. **Налаштуйте моніторинг:**
   - Dashboard → **Insights** (CPU, Memory, Requests)
   - Налаштуйте **Alerts** (email при високому CPU/памʼяті)

2. **Створіть бекапи:**
   - PostgreSQL автоматичні бекапи (включено)
   - Volume snapshots: Settings → Storage → Create Snapshot

3. **Оптимізуйте продуктивність:**
   - Додайте Redis для кешування (Managed Database)
   - Налаштуйте CDN для статики
   - Увімкніть autoscaling при зростанні трафіку

4. **CI/CD вже працює!**
   - Кожен `git push origin main` автоматично деплоїть
   - Rollback доступний у Actions → Rollback

---

## 📚 Додаткові ресурси

- **Повна документація**: `DIGITALOCEAN_DEPLOYMENT.md`
- **App Platform Docs**: https://docs.digitalocean.com/products/app-platform/
- **doctl CLI**: https://docs.digitalocean.com/reference/doctl/
- **Pricing**: https://www.digitalocean.com/pricing/app-platform

---

## 🆘 Потрібна допомога?

- **DigitalOcean Support**: https://cloud.digitalocean.com/support
- **Community**: https://www.digitalocean.com/community
- **Status Page**: https://status.digitalocean.com/

---

**Вітаємо з успішним деплоєм! 🎉**

URL вашого застосунку: `https://brainai-online-xxxxx.ondigitalocean.app`
