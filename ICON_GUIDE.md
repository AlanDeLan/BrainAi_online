# Інструкція по роботі з іконками додатку

## 📍 Розташування іконок

### 1. Favicon для веб-інтерфейсу
**Файл:** `static/favicon.ico`
- Використовується в браузері (вкладка, закладки)
- Відображається на веб-сторінці

### 2. Іконка для EXE файлу (Windows)
**Зараз:** Не встановлена (`icon=None` в `run_app.spec`)
- Відображається в проводнику Windows
- Відображається в панелі задач
- Відображається в ярликах

---

## 🔄 Як замінити Favicon

### Крок 1: Підготувати новий favicon
1. Створіть іконку розміром **16x16** або **32x32** пікселів
2. Збережіть у форматі **ICO** або **PNG**
3. Назвіть файл `favicon.ico`

### Крок 2: Замінити файл
1. Скопіюйте новий `favicon.ico` в папку `static/`
2. Замініть старий файл `static/favicon.ico`

### Крок 3: Перезапустити сервер
- Якщо використовуєте Python: перезапустіть сервер
- Якщо використовуєте EXE: перезапустіть додаток

### Крок 4: Очистити кеш браузера
- Натисніть `Ctrl + F5` для оновлення сторінки без кешу
- Або очистіть кеш браузера вручну

---

## 🖼️ Як додати іконку для EXE файлу

### Крок 1: Підготувати іконку
1. Створіть іконку розміром **256x256** пікселів (рекомендовано)
2. Формат: **ICO** (Windows)
3. Можна використати той самий файл, що й для favicon, або створити окремий

### Крок 2: Розмістити іконку
Рекомендовано: розмістити в `static/icon.ico` або використати `static/favicon.ico`

### Крок 3: Оновити `run_app.spec`
Відредагуйте файл `run_app.spec`:

```python
exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='LocalBrain',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='static/favicon.ico',  # ЗМІНІТЬ ЦЕ: додайте шлях до іконки
)
```

### Крок 4: Перезібрати EXE
```powershell
.\build_exe.ps1
```

Або вручну:
```powershell
pyinstaller run_app.spec --clean --noconfirm
```

### Крок 5: Перевірити результат
1. Перевірте, чи з'явилася іконка в `dist/LocalBrain.exe`
2. Перевірте в проводнику Windows
3. Перевірте в панелі задач

---

## 📝 Детальна інструкція

### Вимоги до іконки для EXE:
- **Формат**: ICO (Windows)
- **Розміри**: 16x16, 32x32, 48x48, 64x64, 128x128, 256x256 (в одному файлі)
- **Рекомендований розмір**: 256x256 пікселів
- **Колірна глибина**: 32-bit (RGBA)

### Як створити ICO файл:

#### Варіант 1: Онлайн-інструменти
1. [ConvertICO](https://convertio.co/png-ico/)
2. [ICO Convert](https://icoconvert.com/)
3. [Favicon.io](https://favicon.io/)

#### Варіант 2: Графічні редактори
- **GIMP** (безкоштовний)
- **Photoshop** (платний)
- **Paint.NET** (безкоштовний, з плагіном)

#### Варіант 3: Спеціалізовані програми
- **IcoFX** (безкоштовна)
- **Greenfish Icon Editor** (безкоштовна)
- **ImageMagick** (командний рядок)

### Приклад створення ICO з PNG:
```powershell
# Якщо встановлено ImageMagick
magick convert icon.png -resize 256x256 icon.ico
```

---

## 🔧 Налаштування в коді

### Favicon в HTML (опціонально)
Можна додати посилання на favicon в `templates/index.html`:

```html
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Local Brain</title>
    <link rel="icon" type="image/x-icon" href="/static/favicon.ico">
    <link rel="shortcut icon" type="image/x-icon" href="/static/favicon.ico">
    <link rel="stylesheet" href="/static/style.css">
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
</head>
```

**Примітка:** Зараз favicon автоматично обслуговується через endpoint `/favicon.ico` в `main.py`, тому додавання в HTML не обов'язкове.

---

## ✅ Чеклист заміни іконки

### Для Favicon:
- [ ] Створено новий `favicon.ico` файл
- [ ] Замінено `static/favicon.ico`
- [ ] Перезапущено сервер
- [ ] Очищено кеш браузера
- [ ] Перевірено відображення в браузері

### Для EXE іконки:
- [ ] Створено новий `icon.ico` файл (256x256)
- [ ] Розміщено в `static/icon.ico` або `static/favicon.ico`
- [ ] Оновлено `run_app.spec` (додано `icon='static/icon.ico'`)
- [ ] Перезібрано EXE файл
- [ ] Перевірено відображення іконки в проводнику
- [ ] Перевірено відображення в панелі задач

---

## 🎨 Рекомендації

### Дизайн іконки:
1. **Простота**: Проста іконка краще розпізнається
2. **Контрастність**: Висока контрастність для кращої видимості
3. **Розмір**: Перевірте на різних розмірах (16x16, 32x32, 256x256)
4. **Колір**: Використовуйте помірну кількість кольорів
5. **Стиль**: Збережіть стиль, що відповідає вашому бренду

### Розміри для різних випадків:
- **16x16**: Панель задач, системний трей
- **32x32**: Список файлів, ярлики
- **48x48**: Великі значки
- **256x256**: Детальний перегляд, високоякісне відображення

---

## 🐛 Вирішення проблем

### Проблема: Іконка не відображається в браузері
**Рішення:**
1. Перевірте, чи існує файл `static/favicon.ico`
2. Очистіть кеш браузера (`Ctrl + F5`)
3. Перевірте, чи працює endpoint `/favicon.ico`
4. Перевірте консоль браузера на помилки

### Проблема: Іконка не відображається в EXE
**Рішення:**
1. Перевірте, чи правильний шлях в `run_app.spec`
2. Перевірте, чи існує файл іконки
3. Перевірте формат файлу (має бути ICO)
4. Перезіберіть EXE файл
5. Перевірте, чи іконка містить необхідні розміри

### Проблема: Іконка розмита
**Рішення:**
1. Використовуйте іконку високої якості (256x256)
2. Перевірте, чи іконка містить всі необхідні розміри
3. Використовуйте векторну графіку як основу

### Проблема: PyInstaller не знаходить іконку
**Рішення:**
1. Використовуйте відносний шлях від кореня проекту
2. Перевірте, чи файл існує перед збіркою
3. Додайте іконку в `datas` в `run_app.spec` (опціонально)

---

## 📚 Додаткові ресурси

### Інструменти:
- [Favicon.io](https://favicon.io/) - генератор favicon
- [ICO Convert](https://icoconvert.com/) - конвертер в ICO
- [RealFaviconGenerator](https://realfavicongenerator.net/) - генератор favicon для всіх платформ

### Документація:
- [PyInstaller Icons](https://pyinstaller.readthedocs.io/en/stable/usage.html#cmdoption-icon)
- [Favicon Best Practices](https://www.w3.org/2005/10/howto-favicon)

---

## 📝 Приклад конфігурації

### `run_app.spec` з іконкою:
```python
exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='LocalBrain',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='static/favicon.ico',  # Іконка для EXE
)
```

### `templates/index.html` з favicon:
```html
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Local Brain</title>
    <link rel="icon" type="image/x-icon" href="/static/favicon.ico">
    <link rel="stylesheet" href="/static/style.css">
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
</head>
```

---

**Примітка:** Після зміни іконки для EXE файлу необхідно перезібрати додаток. Favicon можна змінювати без перебудови EXE, але потрібно перезапустити сервер.




