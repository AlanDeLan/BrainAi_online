# 🚀 Інструкція зі збірки .exe файлу

## ✅ Все готово для збірки!

Проект налаштовано для збірки .exe файлу через PyInstaller.

---

## 📋 Швидка інструкція (3 кроки)

### Крок 1: Встановити PyInstaller
```powershell
pip install pyinstaller
```

### Крок 2: Запустити збірку
```powershell
.\build_exe.ps1
```

Або вручну:
```powershell
pyinstaller run_app.spec --clean
```

### Крок 3: Готово!
Файл буде в `dist/LocalGeminiBrain.exe`

---

## 📁 Що було зроблено

### ✅ Додано підтримку PyInstaller:
1. **`run_app_exe.py`** - точка входу для exe версії
2. **`run_app.spec`** - оновлена конфігурація PyInstaller
3. **Підтримка шляхів** - всі файли правильно знаходяться в exe
4. **`build_exe.ps1`** - скрипт автоматичної збірки

### ✅ Виправлено для PyInstaller:
- ✅ Шляхи до шаблонів та статичних файлів
- ✅ Шляхи до промптів
- ✅ Шляхи до `archetypes.yaml`
- ✅ Шляхи до `.env` файлу
- ✅ Шляхи до історії та векторної бази
- ✅ Всі приховані імпорти

---

## 🎯 Після збірки

### Створіть папку:
```
LocalGeminiBrain/
├── LocalGeminiBrain.exe
├── .env (створіть файл)
└── archetypes.yaml (опціонально)
```

### Створіть файл `.env`:
```
GOOGLE_API_KEY=ваш_ключ_від_google
```

### Запустіть:
Подвійний клік на `LocalGeminiBrain.exe`

---

## 📝 Детальна інструкція

Дивіться файли:
- `BUILD_INSTRUCTIONS.md` - повна інструкція
- `BUILD_EXE.md` - детальна документація
- `QUICK_BUILD.md` - швидка інструкція

---

## ⚠️ Важливо

1. **Файл `.env`** має бути поруч з exe
2. **Файл `archetypes.yaml`** може бути поруч для редагування
3. **Папки `history/` та `vector_db_storage/`** створяться автоматично
4. **Розмір exe**: ~100-200 MB (це нормально)
5. **Час збірки**: 2-5 хвилин
6. **Час старту**: 10-30 секунд (перший раз)

---

## 🐛 Вирішення проблем

Якщо виникають проблеми, дивіться `BUILD_EXE.md` розділ "Вирішення проблем".

---

**Готово до збірки! Запустіть `.\build_exe.ps1`**





