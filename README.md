# Local Brain

Інтелектуальний локальний AI-асистент з підтримкою множинних агентів та векторної бази даних.

## 🚀 Особливості

- **Множинні AI-провайдери**: Підтримка Google AI Studio та OpenAI API
- **Архетипи (Агенти)**: Створюйте та налаштовуйте власних AI-агентів з унікальними промптами
- **Режим РАДА**: Обговорення між кількома агентами для комплексного аналізу
- **Векторна база даних**: ChromaDB для збереження та пошуку контексту розмов
- **Веб-інтерфейс**: Сучасний та зручний інтерфейс для роботи з агентами
- **Локальне збереження**: Вся історія та дані зберігаються локально
- **EXE версія**: Готова до використання standalone версія для Windows

## 📋 Вимоги

- **Windows 10/11** (або Linux/Mac для розробки)
- **Python 3.11+** (для розробки)
BrainAi — локальна и онлайн-версия AI-ассистента
===============================================

Краткое описание
-----------------
Это репозиторий проекта BrainAi — мультиагентный AI-ассистент с возможностью работы локально и в облаке. Приложение предоставляет:

- Web-API на FastAPI
- Подключение к нескольким AI-провайдерам (Google AI Studio / OpenAI / Claude)
- Векторное хранилище для контекста (ChromaDB/FAISS)
- Веб-интерфейс и опции для упаковки в exe

Кому это пригодится
------------------
- Разработчикам, которые хотят локального AI-агента
- DevOps-инженерам, которые деплоят приложение на Render/Heroku/VM
- Исследователям, кто работает с векторными представлениями и памятью диалогов

Структура репозитория
---------------------
- `main.py`, `main_production.py` — точки входа (dev/prod)
- `core/` — логика, провайдеры, настройки, аутентификация
- `vector_db/`, `vector_db_storage/` — клиенты и хранилище векторов
- `templates/`, `static/` — фронтенд
- `tests/` — автоматические тесты

Быстрый старт (локально)
-----------------------
1. Перейдите в папку проекта:

```powershell
Set-Location -Path "C:\Users\alan\OneDrive\IT_project\GitHub\BrainAi_online"
```

2. Создайте виртуальное окружение и установите зависимости:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install --upgrade pip
pip install -r requirements.txt
```

3. Скопируйте пример файла окружения и отредактируйте значения (НЕ коммитьте реальные ключи):

```powershell
Copy-Item .env.example .env
notepad .env
```

4. Запустите в режиме разработки:

```powershell
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Production и деплой
-------------------
- Gunicorn + Uvicorn для production: смотрите `gunicorn_config.py` и `main_production.py`.
- Для Render: в репозитории есть `render.yaml` — настройте переменные окружения в панели Render и деплойте через GitHub.

Переменные окружения
---------------------
Обязательные (в зависимости от конфигурации):

- `SECRET_KEY` — секрет для JWT/сессий
- `AI_PROVIDER` — `google_ai` | `openai` | `claude`
- Ключи провайдеров: `OPENAI_API_KEY`, `GOOGLE_API_KEY` и т.д.
- `DATABASE_URL` — если используется PostgreSQL

Безопасность и конфиденциальность
---------------------------------
- Никогда не добавляйте `.env` или секреты в репозиторий.
- Если секреты уже попали в историю — немедленно их отозвать и очистить историю (`git filter-repo` или BFG). См. `SECURITY.md`.
- Включите ограничение CORS, Trusted Hosts и rate limiting для публичного деплоя.

Тесты
-----
Запуск тестов:

```powershell
pytest -q
```

Полезные команды
----------------
Клонировать:

```powershell
git clone https://github.com/AlanDeLan/BrainAi_online.git
```

Создать ветку и отправить PR:

```powershell
git checkout -b update/readme
git add README.md
git commit -m "docs: update README"
git push origin update/readme
```

Если push отклоняется (pre-receive hook) из-за больших файлов — используйте `git filter-repo` или BFG для удаления больших артефактов из истории и затем force-push.

Контакты и вклад
----------------
Если хотите внести изменения — откройте issue или PR. Для вопросов используйте страницу Issues в GitHub репозитории.

Лицензия
--------
Добавьте файл `LICENSE` в корень репозитория, если планируете открытый релиз.

Обновлено: 13 ноября 2025 г.
- Початкові думки кожного агента

- Обговорення між агентами

