# 📦 Інструкція зі збірки .exe файлу

> 💡 **Цей файл застарів. Дивіться [BUILD_EXE.md](BUILD_EXE.md) для актуальної інструкції.**

## Швидкий старт (3 кроки)

### Крок 1: Встановити PyInstaller
```powershell
pip install pyinstaller
```

### Крок 2: Запустити скрипт збірки
```powershell
.\build_exe.ps1
```

### Крок 3: Готово!
Файл буде в `dist/LocalBrain.exe`

---

## Детальна інструкція

> ⚠️ **Для детальної інструкції дивіться [BUILD_EXE.md](BUILD_EXE.md)**

### Передумови
- ✅ Python 3.11+ встановлений
- ✅ Всі залежності встановлені (`pip install -r requirements.txt`)
- ✅ Файл `.env` з `GOOGLE_API_KEY` готовий

### Автоматична збірка (Рекомендовано)

1. **Відкрийте PowerShell в папці проекту**

2. **Запустіть скрипт збірки:**
   ```powershell
   .\build_exe.ps1
   ```

3. **Очікуйте завершення** (2-5 хвилин)

4. **Результат:**
   - Файл: `dist/LocalGeminiBrain.exe`
   - Розмір: ~100-200 MB

### Ручна збірка

```bash
# 1. Встановити PyInstaller
pip install pyinstaller

# 2. Зібрати exe
pyinstaller run_app.spec --clean

# 3. Результат в dist/LocalGeminiBrain.exe
```

---

## Після збірки

### 1. Створіть папку для розповсюдження:
```
LocalGeminiBrain/
├── LocalGeminiBrain.exe
├── .env (створіть вручну)
└── archetypes.yaml (опціонально - для редагування агентів)
```

### 2. Створіть файл `.env`:
```
GOOGLE_API_KEY=ваш_ключ_від_google
```

**Важливо:** Файл `.env` має бути поруч з exe файлом!

### 3. (Опціонально) Скопіюйте `archetypes.yaml`:
Якщо хочете редагувати агентів через інтерфейс, скопіюйте `archetypes.yaml` поруч з exe.
Якщо не скопіюєте - програма використає версію з ресурсів (read-only).

### 4. Запустіть програму:
Подвійний клік на `LocalGeminiBrain.exe`

### 5. Перевірте роботу:
- Відкриється браузер з інтерфейсом
- Сервер працює на http://127.0.0.1:8000
- Можна обирати агентів та використовувати режим РАДА

---

## Структура файлів для exe

### Включені файли:
- ✅ `templates/` - HTML шаблони
- ✅ `static/` - CSS/JS файли
- ✅ `prompts/` - файли з промптами
- ✅ `archetypes.yaml` - конфігурація агентів
- ✅ Всі Python модулі та залежності

### Створюються автоматично:
- 📁 `history/` - історія чатів (поруч з exe)
- 📁 `vector_db_storage/` - векторна база (поруч з exe)

---

## Налаштування збірки

### Змінити назву exe:
Відредагуйте `run_app.spec`:
```python
name='ВашаНазва',  # Замість 'LocalGeminiBrain'
```

### Додати іконку:
1. Підготуйте `.ico` файл
2. Відредагуйте `run_app.spec`:
```python
icon='static/favicon.ico',
```

### Приховати консоль:
Відредагуйте `run_app.spec`:
```python
console=False,  # Замість True
```

### Оптимізувати розмір:
Відредагуйте `run_app.spec`:
```python
upx=False,  # Вимкнути стиснення (швидший старт)
optimize=2,  # Максимальна оптимізація
```

---

## Вирішення проблем

### ❌ "ModuleNotFoundError"
**Рішення:** Додайте модуль в `hiddenimports` в `run_app.spec`

### ❌ "FileNotFoundError: archetypes.yaml"
**Рішення:** Переконайтеся, що файл включений в `datas` в `run_app.spec`

### ❌ Великий розмір exe (>200 MB)
**Примітка:** Це нормально для Python додатків. Всі залежності включені.

### ❌ Повільний старт
**Рішення:** Це нормально для PyInstaller. Перший запуск може займати 10-30 секунд.

### ❌ Антивірус блокує exe
**Рішення:** Додайте exe файл в винятки антивірусу. Це нормально для PyInstaller.

### ❌ Порт 8000 зайнятий
**Рішення:** Закрийте інші програми, що використовують порт 8000, або змініть порт в коді.

---

## Тестування

### Локальне тестування:
1. Запустіть `dist/LocalGeminiBrain.exe`
2. Перевірте:
   - ✅ Відкривається браузер
   - ✅ Сервер запускається
   - ✅ Завантажується інтерфейс
   - ✅ Працює вибір агентів
   - ✅ Працює режим РАДА
   - ✅ Зберігається історія

### Тестування на іншому комп'ютері:
1. Скопіюйте `LocalGeminiBrain.exe` та `.env`
2. Запустіть на чистому Windows (без Python)
3. Перевірте всі функції

---

## Розповсюдження

### Мінімальний набір:
```
LocalGeminiBrain.exe
.env (з GOOGLE_API_KEY)
README.txt (інструкція)
```

### Рекомендований набір:
```
LocalGeminiBrain/
├── LocalGeminiBrain.exe
├── .env
├── README.txt
└── SCREENSHOTS/ (скріншоти інтерфейсу)
```

---

## Примітки

- **Розмір**: 100-200 MB (це нормально)
- **Час збірки**: 2-5 хвилин
- **Час старту**: 10-30 секунд (перший раз)
- **Python версія**: 3.11+ рекомендовано
- **ОС**: Windows 10/11

---

## Додаткова інформація

- `BUILD_EXE.md` - детальна документація
- `PACKAGING_OPTIONS.md` - всі варіанти упаковки
- `run_app.spec` - конфігурація PyInstaller

